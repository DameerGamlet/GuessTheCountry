<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.guessthecountry">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_icoico"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_icoico_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GuessTheCountry">
        <activity
            android:name=".GuessAsianGerb"
            android:label="@string/game4" />
        <activity
            android:name=".Settings"
            android:label="@string/sett"/>
        <activity
            android:name=".GuessAsiaCountries"
            android:label="@string/game2" />
        <activity
            android:name=".GuessEuroFlags"
            android:label="@string/game3"/>
        <activity
            android:name=".GuessTheFlag"
            android:label="@string/game1" />
        <activity
            android:name=".Levels"
            android:label="@string/ch"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>